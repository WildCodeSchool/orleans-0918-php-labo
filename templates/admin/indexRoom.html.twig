{% extends 'baseAdmin.html.twig' %}

{% block title %}Gestion des Salles{% endblock %}

{% block body %}
<div class="container">

        <h2 class="text-center mt-5 mb-5">Liste des salles</h2>

    <div class="row">
        <div class="col-md-12">
            <table id="datatable" class="table table-striped table-bordered" cellspacing="0" width="100%">
                <thead class="text-center">
                <tr>
                    <th>N° de la salle</th>
                    <th>Nom de la salle</th>
                    <th>Nombre de portes</th>
                    <th>Modifier</th>
                    <th>Supprimer</th>
                </tr>
                </thead>

                <tbody class="text-center">
                {% for room in rooms %}
                <tr>
                        <td>{{ room.id }}</td>
                        <td class="text-left">{{ room.name }}</td>
                        <td>{{ room.door }}</td>
                    <td><a href="#" class="badge badge-light"><i class="fas fa-pen"></i></a></td>
                    <td><a href="#" class="badge badge-light"><i class="fas fa-trash-alt"></i></a></td>
                </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}