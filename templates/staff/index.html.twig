{% extends 'baseAdmin.html.twig' %}

{% block title %}Staff index{% endblock %}

{% block body %}
    <main>
        <div class="container mt-5">
            <h2 class="text-center mt-5 mb-5">Liste du Personnel</h2>
            <div class="row">
                <div class="col-md-12">
                    <table id="datatable" class="table table-striped table-bordered">
                    <thead class="thead-center text-center">
                    <tr>
                        <th>Nom</th>
                        <th>Prénom</th>
                        <th>Modifier</th>
                        <th>Supprimer</th>
                    </tr>
                    </thead>
                    <tbody class="text-center">
                    {% for staff in staff %}
                        <tr>
                            <td>{{ staff.lastname }}</td>
                            <td>{{ staff.firstname }}</td>
                            <td><a href="{{ path('staff_edit', {'id': staff.id}) }}" class="badge badge-light"><i class="fas fa-pen"></i></a></td>
                            <td><a href="{{ path('staff_delete', {'id': staff.id}) }}" class="badge badge-light"><i class="fas fa-trash-alt"></i></a></td>
                        </tr>
                    {% endfor %}
                    </tbody>
                    </table>
                    <div class="row justify-content-center validbutton">
                        <a href="{{ path('staff_new') }}" class="btn text-center style-button bg-gradient1 mx-auto"><span>Ajouter</span></a>
                    </div>
                </div>
            </div>
        </div>
    </main>

{% endblock %}
